# Backlog

## Backlog

Roughly priority order, top to bottom. The small, well-specified wins that used to open
this list (the arrow/LinkedIn consistency pass, the CI version bump, the CTA-prominence
pass) are done, see Done below, and so are the first passes of the case-study refine and
image work, and so are the Lightbox caption treatment and the WCAG contrast fix. What's left
is lower-priority or longer-horizon.

- [ ] Case study second pass: refine content and re-export images together, one case study at a time. Combines the second passes of "Refine case study content" and "Case study image assets" (both first passes are in Done). Decided 2026-09-18: Jim finds it a good workflow to tighten a page's content and re-export its images in the same sitting, rather than as two separate sweeps. One worktree and branch per case study, as in the first pass (`fix/refine-case-study-<id>`).
  - Order: tbd, Jim's call. The 2026-09-14 note in the first-pass item flagged the first 4 (Operational Insights, Agentic AI Chat, Genomic Data Platform, AI Recommendations) for a further pass ahead of the remaining 5, which were done first; all 4 also predate the 2x export standard (2026-09-16).
  - Method per page: same as the first pass. Full source review (principle 10), refine, re-export, record `width`/`height`, verify with `npm run build` and the Playwright one-line caption check. Working notes stay in `planning/refine-case-studies.md`.
  - Carries over from the first pass: (a) images still on 100%-scale exports, both Pillow passes (no cwebp/ImageMagick in this environment) and older re-exports, get re-exported at 2x from the original design files, which turns the first pass's "bump to 2x opportunistically when a page is next touched" rule into the plan (twice the display width, so 1744px unconstrained or `2 * maxWidth`, see principle 7); (b) open flags recorded per page in `planning/refine-case-studies.md`, notably Analytics & Big Data (confirm the shipped Orchestration Editor matches the storyboard's red/green connection interaction, confirm the Analytics Catalog identification, whether the 2013 workshop-room photo `GE-SR0012.JPG` belongs to this project, three different product names across the artifacts) and Machine Learning for Operators (AI Workbench vs. Analytics Workbench naming, unresolved); (c) captions: the Lightbox caption restyle (see Done) kept the ~90-100 character one-line budget, so no caption rewrites are needed on its account; keep writing to it and re-run the wrap check as each page is touched.

- [ ] Restore case study video content — the previous site (jimreedia.myportfolio.com) **embedded its videos from Vimeo** (Jim's Vimeo account is the canonical home for the source clips). The rebuild dropped them or replaced them with stills. Known cases: **Samsung Interactive TV** ("Video Mockups" — After Effects motion studies), **Developer Experience** (hands-on coding-tip web videos + After Effects motion prototypes; `Predix/cloud foundry/PredixIO_nav_behavior.mov` also exists locally), **Mobile Emergency System** ("Motion Study" — the current `motion-study.png` is actually a screen-flow diagram, not the animation). Also fold in the remaining animated GIF — `AI Recommendations/AI-Recommendations_animated-behaviors.gif`. Scope: (1) pull the Vimeo list and map each clip to its case study (featured + more); (2) decide hosting — keep Vimeo embeds vs. self-host MP4/WebM (see the hosting note below); (3) add a `video` block type to `CaseStudyPage.jsx` / the `blocks` schema (poster frame; short silent motion-study loops can autoplay-muted-loop, longer demos get controls; respect `prefers-reduced-motion`; if Vimeo, use a lazy `<iframe>` with `dnt=1` and a click-to-play poster so it isn't an unfetched third-party frame on load — mind the CSP if one gets added); (4) place each video in its narrative section and update `caseStudies.json`. Coordinate with the "Case study image assets" WebP pass and the hand/gear GIF→CSS item.
  - Done for Operational Insights (2026-09-12, via the "Refine case study content" item): `Operational Insights/OI-Slack-app.gif` is in on a new "What I Tried Based on What I Heard From Customers" section, copied straight in as `slack-app.gif` and rendered via a plain `<img>` (GIFs animate natively, no `video` block type needed for this one). Not yet addressed: it autoplay-loops with no `prefers-reduced-motion` handling, since a GIF's own frame animation can't be paused via CSS media query alone; would need a JS-driven play/pause affordance if that matters here. Samsung and Banking Experiences entries were removed from the site entirely (2026-09-11, see the "Refine case study content" item), so their video needs no longer apply.
  - **`video` block type shipped (2026-09-16), first real use on Developer Experience.** `PredixIO_nav_behavior.mov` (local file, 13s, silent, 1024×768) turned out to already be live on Jim's Vimeo account (id `268208418`, title/author/upload-date all matched, found via `vimeo.com/api/oembed.json`) and was the exact clip embedded under myportfolio's old "Motion Design" topic, confirmed by scraping the myportfolio page's raw HTML for the `player.vimeo.com` iframe src. Used the existing Vimeo asset rather than re-hosting: added a `video` block type (`provider`, `vimeoId`, `poster`, plus the usual `width`/`height`/`alt`/`caption`) to `CaseStudyPage.jsx` and `src/index.css`, rendered as a lazy click-to-play poster (Vimeo's own oEmbed thumbnail, pulled at 960px and saved as `nav-behavior-poster.webp`) that swaps in the real `<iframe>` on click, `dnt=1`, and the same `sandbox` attribute values the myportfolio embed used. Wired into the "Lo-Fi Wireframes and Motion Prototypes" section, right after the wireframes image, since that section already covers the old "Motion Design" ground in prose. **Known limitation:** this sandboxed environment's outbound network gets a Cloudflare bot challenge (401, "we couldn't verify the security of your connection") on `player.vimeo.com` regardless of referer/user-agent, so actual playback couldn't be verified end-to-end here, only the poster render, click handler, and resulting iframe `src` (all confirmed correct via Playwright). Verify playback in an unrestricted browser before treating this as fully done. Samsung's motion studies and Mobile Emergency System's motion study remain open.
  - Hosting opinion (Claude, 2026-09-09): **keep Vimeo for the longer walkthrough/demo videos, self-host the short silent motion-study loops.** Vimeo pros: zero bandwidth/egress cost and no encoding work, adaptive streaming so a 2-minute demo doesn't stall on slow connections, the clips already live there, `dnt=1` disables Vimeo's tracking, and no ads/recommendations like YouTube. Vimeo cons: the free tier shows Vimeo branding and can gate embeds/quality, the player is ~600KB+ of third-party JS + cookies (privacy-mode helps but doesn't zero it), it's a dependency that can rebrand/reprice/die, and the chrome never fully matches the site. For a 3-second UI transition that just loops, an embed is absurd overhead — a 200–500KB self-hosted MP4/WebM with `autoplay muted loop playsinline` is smaller than the player bootstrap and fully under Jim's control. Rule of thumb: loops and micro-interactions → self-host; anything with narration, longer than ~15s, or that a viewer would scrub → Vimeo embed (lazy, click-to-play).

- [ ] Optimize mobile experience — review and adjust vertical spacing throughout, and consider implementing animated scroll transitions for mobile (the Featured Case Studies column reveal and PacMan chase are currently desktop-only)

- [ ] Case study tags: review/adjust the tagging strategy if needed, then populate real tag values for each case study
  - As of the More Case Studies rebuild (branch: feature/add-more-case-studies-section), tag rendering is removed from the More cards; the featured section never rendered tags. The existing `tags` arrays are still in `caseStudies.json` untouched. To do: settle the strategy, set real values, and reintroduce a tag UI (featured and/or more).

## In Progress

## Done

- [x] Give the red PacMan ghost a rare pupil glance, done 2026-09-23 (branch `feature/red-ghost-pupil-glance`). New `pacman-pupil-glance` keyframes on `.pacman__layer--red .pacman__pupils` in `src/index.css`: a 45s cycle (vs. orange's 16s) that holds at rest (`LOOK.right`) and, once per cycle, eases to centre (`translateX(-18px)`) in ~0.3s, holds ~1.9s, and eases back. The glance sits mid-cycle (44-49.7%) so the first one lands ~20s after load rather than ~40s. Reduced-motion guard added alongside orange's.
  - Open question resolved: red's pupils also lock at rest during the chase (added to the existing `.pacman-chasing` rule), so red keeps looking back at its pursuer while fleeing instead of glancing mid-run.
  - Verified in Playwright by seeking the animation: pupil sits at offset 36 (rest) at 0s/10s/22.5s/30s and 18 (centre) at 20.5s/21.5s, confirming the sign is correct; `animation-name` computes to `none` under `.pacman-chasing` and under `prefers-reduced-motion: reduce`; `npm run build` clean.

- [x] Modify the carousel controls and interactions of the featured case studies images on the homepage — done 2026-09-23 (branch `feature/carousel-controls`). `src/components/Carousel.jsx`, `src/components/FeaturedCaseStudies.jsx`, `src/components/MoreCaseStudies.jsx`, `src/components/CaseStudyPage.jsx`, `src/components/Home.jsx`, `src/lib/homeScroll.js`, `src/index.css`.
  - **Image is now a real link to the case study**, matching `.more-work__thumb`'s hover treatment exactly (1.05 scale, dark scrim fade-in, "View Case Study →" label fade/slide-up, touch fallback gradient). Since the arrow/dot controls moved off the image entirely (see below), the nested-`<button>`-inside-`<a>` conflict the original note worried about never materialized: `carousel__frame` is simply the `Link` itself, wrapping the image track and overlay, the same structure as `.more-work__card` → `.more-work__thumb` — no absolute-position sibling trick needed. Confirmed with Jim before implementing.
  - **Controls relocated to a single pill container below the frame**: prev arrow, dot row, next arrow, all always visible (no hover-reveal), sitting on the Featured section's own `--color-surface-mid` background. After an initial pass gave the arrows their own individual circles with the dots floating free, Jim asked for the whole control set inside one shared container instead — `.carousel__controls` now carries the pill (`--color-surface-light` fill, `999px` radius) and the arrows lost their individual backgrounds, chevron `--color-frame` at rest / `--color-accent` on hover-focus. Dots keep their existing sizing (6px/8px, `--color-frame`/`--color-accent`) but are now real `<button>`s.
  - **Hit areas enlarged independently of the visible pill size**: Jim wanted bigger click/tap targets but the same small, tight visual footprint (not the larger pill an initial attempt produced). Each arrow/dot keeps its original visible size but gets a transparent `::after` positioned with a negative `inset` to extend its actual clickable region — 36×36px for arrows (from 20px), 14–16px for dots (from 6–8px) — sized so adjacent controls' hit areas meet exactly at the midpoint of their gap and never overlap. Verified geometrically with Playwright (measured `::after` boxes, confirmed zero overlap and, after a follow-up request to close the dead zone between each arrow and the dots, zero gap between every adjacent hit area too — the arrow-to-dots flex gap was tightened from 16px to 8px and the arrows' `::after` insets made asymmetric, 8px on the outer side and 4px facing the dots, to match).
  - **True infinite loop both directions**: `slides` now prepends the last image and appends the first (`[images[count-1], ...images, images[0]]`), starting at `index = 1`; `handleTransitionEnd` snaps back across either duplicated end without animating. Neither arrow is ever disabled.
  - **Dots are clickable**, jumping straight to that image via the existing slide transition (confirmed with Jim: sliding across intermediates, not an instant snap).
  - **Touch swipe is direction-aware**: swipe left → next, swipe right → previous (previously any swipe past the threshold advanced forward only).
  - **Added to this PR at Jim's request: "Back to all work" restores the exact homepage scroll position** when the case study was reached directly from a homepage click (Featured carousel image, Featured CTA, or a More Case Studies card) — confirmed with Jim that this applies to both Featured and More, not just Featured. Reaching the page instead via the Prev/Next nav at the bottom of a case study keeps the original behavior (scrolls to the top of the `featured-work`/`more-work` section), since at that point the visitor is no longer "returning" to a spot they scrolled to. Mechanism: `src/lib/homeScroll.js` saves `window.scrollY` to `sessionStorage` in an `onClick` on each homepage-to-case-study link (dynamic, captured at click time); those same links also pass static `state={{ fromHome: true }}`, which — unlike `sessionStorage` — resets on every navigation, so a Prev/Next hop away loses it and "Back to all work" naturally falls through to the section-top default. `CaseStudyPage.jsx` reads both to decide whether `backScrollTo` is a saved number or the existing section-id string; `Home.jsx`'s existing `location.state.scrollTo` handler grew a branch for the numeric case (exact `window.scrollTo`) alongside its existing section-id (`scrollIntoView`) and `'top'` cases.
  - Verified: `npm run build` clean throughout. Playwright checks against the dev server (desktop 1440×900 and mobile 390×844): image `href` resolves to the case study route, hover shows the scrim/label at opacity 1, prev from the first image wraps to the last and next from the last wraps to the first, a full next-loop returns to the start, dot clicks jump to the right slide, no console errors on either viewport, keyboard Tab order is link → prev → each dot → next with Enter activating a focused dot. Scroll restoration: confirmed exact-position round-trips from the Featured carousel, the Featured CTA, and a More Case Studies card, and confirmed a Prev/Next hop before "Back to all work" lands at the section top (accounting for the existing `scroll-margin-top` nav-clearance offset) rather than the originally-scrolled-from position. Dev server left running (`localhost:5185`) for Jim's own visual pass.

- [x] Refactor the hand/gear animation to CSS instead of a GIF, and make it clickable (gear bursts into confetti) — done 2026-09-22 (branch `feature/hand-gear-confetti`). `BrandImagery.jsx`'s `.brand-imagery__illustration` `<img>` (the 130KB GIF) is replaced by a new `HandGearMark.jsx` component (inline SVG, hand and gear as separate paths) plus real DOM text for `jimreed.net` (Roboto Slab 700) / `UX Design` (Roboto Slab 500, `var(--color-text-muted-dark)`) — both previously baked into the GIF's pixels, "UX Design" in a mismatched serif.
  - **Vector source deviation:** the backlog's pointer to `brand/hand gear/hand_gear.ai` / `.psd` turned out to be stale — neither file exists anywhere in this repo or its git history, only `public/assets/brand/social-preview.png` (1200×630, text-free) and the GIF itself. Confirmed with Jim: auto-traced `social-preview.png` instead of blocking on sourcing the real `.ai`. Method: OpenCV contour trace (`cv2.findContours` with `RETR_CCOMP`, thresholded at ~140) cleanly separated the gear (solid ring + circular hole) from the hand (stroke-only outline, whose outer/inner edges form a natural outer-contour/hole pair) into two independent, self-contained path/viewBox pairs. `cv2.approxPolyDP` alone (no spline-smoothing pass) gave the cleanest result — an earlier attempt adding a `scipy` B-spline smoothing step actually introduced small cusp/loop artifacts at the fingertip and the finger/thumb notch (spline ringing at high-curvature points); the plain simplified polygon traced faithfully with no artifacts and reads as smooth at real render size. The gear's center hole is punched via a true SVG arc subpath with `fill-rule="evenodd"`, not an overlay shape, so it stays transparent regardless of background or z-order.
  - **Update (2026-09-22): the real `.ai` file existed after all**, just outside this repo (`/Users/jimreed/Projects/portfolio/brand/hand gear/hand_gear.ai` — a sibling of `dev/`, not inside it or in git). Jim asked for a smoother hand shape after seeing the raster trace; re-derived both paths from the actual vector artwork instead of re-tuning the OpenCV trace. The `.ai` is a PDF-compatible Illustrator file, so PyMuPDF's `page.get_drawings()` reads its real cubic-bezier path data directly (no tracing/thresholding needed) — 640×359pt page, gear and hand each their own `f`-type drawing with 2 subpaths (outer + hole), extracted straight to SVG `M/L/C/Z` path data at each drawing's own local origin. One gotcha: PyMuPDF's drawing coordinates are already Y-down (device space), not raw PDF Y-up user space — an initial Y-flip (copying the raster script's logic) rendered the hand upside down; removing it fixed it. Visibly smoother curves than the polygon trace, especially at the fingertip and knuckle bumps. `GEAR_D`/`HAND_D` and the hand/gear positioning percentages in `HandGearMark.jsx` / `index.css` were both updated; the union-bbox math came out within ~1% of the raster trace's, confirming the two sources agree on the composition.
  - **Click → confetti burst, on a real `<button aria-label="Play confetti animation">`** wrapping the gear (hand is a separate, non-interactive `<svg>`, so only the gear hides/bursts). State machine (`idle` → `bursting` → `hidden` → `idle`, ~900ms burst + ~1500ms regrow): gear fades out and 16 hand-authored (not `Math.random()`, so the burst is deterministic/reproducible) confetti pieces fly outward and fade, colors round-robining a legible subset of brand tokens (`--color-accent`, `--color-accent-on-light`, `--color-text-bright-on-dark`, `--color-frame`, `--color-surface-light`, `--color-surface-mid` — the near-black/navy surface tokens were excluded, they'd vanish against the stage's own dark background). A second click mid-burst is a no-op until back to idle.
  - **Scope added during planning: the gear grows slightly larger each time it regrows**, so repeated clicks in one visit playfully "grow" it, capped at a max (`MAX_GROWTH_LEVEL = 6`, `GROWTH_STEP = 0.15`, applied as `transform: scale()` on the button so the tap target grows with it). Confirmed with Jim: only the gear grows (hand stays put as a stable anchor), and growth is plain in-memory state that resets on page reload, not persisted.
  - Idle motion (gear spin 12s linear infinite, hand float 3.2s ease-in-out infinite) and the confetti flight are both wrapped in `@media (prefers-reduced-motion: no-preference)`, matching how the sibling `.cta-arrow` bounce in the same file already opts motion in. Under `prefers-reduced-motion: reduce`: no idle motion, no confetti pieces, and a burst is still acknowledged with an instant fill-color flash on the gear rather than a dead click. New `:focus-visible` ring on the gear button (the codebase had none to reuse elsewhere — the global `button` reset strips native outline).
  - **Regression caught and fixed during verification:** `PacmanScene.jsx`'s root `<svg>` shares the `brand-imagery__illustration` class with the old GIF specifically to get the same 45%/480px sizing budget, splitting the hero row between the two. Renaming that class to `brand-imagery__lockup` (without also updating `PacmanScene.jsx`) let the Pac-Man scene render unconstrained at its natural ~1152px width, squeezing the new lockup down to ~200px and pushing the wordmark text past the viewport edge — invisible with the old flexible raster GIF (which just shrank as one unit) but a hard overflow with real, non-shrinking DOM text. Fixed by keeping `.brand-imagery__illustration` and `.brand-imagery__lockup` sharing the same width rule, so `PacmanScene.jsx` didn't need to change. Caught via Playwright screenshot comparison against the pre-change layout at 1440px, not caught by `npm run build` (a layout regression, not a build error).
  - Verified: `npm run build` clean; Playwright at 1440px desktop and 390px mobile (idle, mid-burst, hidden, regrown, and a second growth step, all screenshotted); idle CSS transforms confirmed actually animating (sampled `getComputedStyle` at two points in time); keyboard Tab-reachability and `aria-label`/`type="button"` confirmed via a fresh-page tab-order walk; `prefers-reduced-motion: reduce` emulation screenshotted (instant color flash, no flight, no idle motion); mobile gear tap target measured at 62×62px (clears the 44px minimum); no console/page errors during the full click/burst/regrow cycle. Not yet done: a real-device visual pass and a final eyeball of the traced paths at very large viewport widths (Jim to review in the running dev server).
  - **Composition fix (2026-09-22):** after seeing it live, Jim flagged the hand/gear as too large relative to the wordmark, too little spacing between them, against a reference screenshot of the original GIF-based layout at a wide viewport. Measured the reference precisely (pixel-scanned the screenshot for the mark's and both text lines' bounding boxes, cross-checked the screenshot's DPR via a known-size element — the PacMan ghosts' eye-white rects — before trusting the conversion to CSS px) and matched `index.css` to it: `.brand-imagery__mark` flex-basis 38% → 24% of the lockup, `.brand-imagery__lockup` gap 28px → 50px and `align-items: center` → `flex-end` (the reference sits the wordmark near the mark's bottom, not centered against its full height), wordmark font sizes 28/18px → 40/28px desktop (with a proportional 27/19px mobile override so the bigger text doesn't wrap at the 320px mobile cap). Confirmed via Playwright measurement that the rebuilt mark/gap/text widths land within a few px of the reference's, and re-verified the click/burst/regrow cycle still works at the smaller gear size.
  - **Composition fix, round 2 (2026-09-22):** still two gaps from the reference: not enough space between the mark and the PacMan ghosts, and (after switching to `align-items: flex-end` above) the wordmark sat too low — Jim wanted "jimreed.net" level with the hand's knuckles, not the mark's bottom edge. Fixes: `.brand-imagery .section__inner` gap 80px → 120px (measured the reference's ghost-to-mark gap, subtracted PacmanScene's own intrinsic ~44px of empty canvas past the ghost — calculated from its `viewBox`, unrelated to this change — to isolate the pure flex-gap value); `.brand-imagery__lockup` `align-items` back to `center`, with a `margin-top: 95px` (66px at the 767px breakpoint) on `.brand-imagery__wordmark` as a calibrated nudge down from dead-center. That margin value isn't eyeballed: because a flex item's margin-box is what gets centered, adding asymmetric `margin-top` with no `margin-bottom` only moves the content down by *half* the margin added (`content_top = (markHeight − (wordmarkHeight + margin)) / 2 + margin`), so the desktop/mobile values were solved algebraically from the reference's measured target (jimreed.net's center at 62.65% down the mark's height) rather than trial-and-error — verified afterward via Playwright at 0.6260 and 0.6272 respectively against the 0.6265 target.
  - **Font swap (2026-09-22):** at Jim's request, "jimreed.net" and "UX Design" traded typefaces — name is now `--font-sans` (Roboto) 700, tag is `--font-display` (Roboto Slab) 500 at 26px (down from 28px, also by request). Desktop-only; the mobile override still reads 19px, not yet reproportioned to the new 26px base since that wasn't asked for.
  - **Small follow-up tweaks (2026-09-22):** the desktop `translateX` lockup nudge from round 3 got walked back by hand, in 20px/10px/10px steps, from 80px down to 40px (Jim eyeballing each step); the lockup's internal `gap` also came down 4px (50→46) to nudge just the wordmark left relative to the mark, leaving the mark's own position untouched. Briefly added `#FF0000`/`#FF7F00` (the PacMan ghost colors) to the confetti palette, then reverted at Jim's request — net no visual change from before.
  - **`--color-text-muted-dark` warmed (2026-09-22):** `#8899BB` → `#909CA7`, at Jim's request, to read closer to the warm neutral light-surface tokens (`#F2F0ED`/`#E5E2DE`) instead of a cooler blue-grey. Halved the R-vs-B channel gap (-51 → -23) rather than fully neutralizing it, since the token still needs to read as part of the same family as `--color-accent`. Contrast improved slightly as a side effect: 6.76:1 → 6.92:1 on `--color-surface-dark`, 5.16:1 → 5.29:1 on `--color-surface-navy` (its three call sites: the "UX Design" tag, the CTA arrow, and the footer copyright line).
  - **Gear hover state (2026-09-22):** `.brand-imagery__gear-btn:hover .brand-imagery__gear` fades its fill to `--color-text-muted-dark` over 200ms, reusing the same warmed token above.
  - **Fixed a responsive dead zone (2026-09-22):** a flat 40px/26px desktop wordmark size, unchanged until the 767px mobile breakpoint dropped it to 27px/19px, left "jimreed.net" clipping past the viewport edge at intermediate widths (~768-895px) — the mark's own 24%-of-lockup width shrinks smoothly with the viewport in that range, but the fixed-px text didn't, until it suddenly could at the breakpoint. Replaced the flat desktop `font-size` with `clamp(27px, 6.2px + 2.70833vw, 40px)` (name) / `clamp(19px, 7.8px + 1.45833vw, 26px)` (tag), solved algebraically so the clamp floor exactly equals the old mobile fixed values (768px endpoint) and the ceiling exactly equals the old desktop values (1248px endpoint, where the lockup hits its 480px cap) — continuous scaling in between, no visible seam at either boundary. The now-redundant mobile font-size override was removed. Verified zero overflow from 390px through 1600px viewport width.
  - **Mobile (≤767px) composition fix (2026-09-22):** a screenshot of the production GIF version's mobile layout showed more vertical space between the PacMan labels and the mark, and the lockup sitting further right, than this rebuild. Measured the reference the same way as the earlier desktop composition passes (pixel-scanned, DPR-calibrated against the nav mark's known 28px size) and found: vertical gap ~85.5px (was 48px), lockup's left edge landing near where a *content-sized* (not a flat 320px-wide) box would center it. Changes: `.brand-imagery .section__inner`'s mobile `gap` 48px → 85px; `.brand-imagery__lockup` no longer takes the shared `width:100%;max-width:320px` rule (split that selector so `.brand-imagery__illustration`/PacmanScene keeps it); `.brand-imagery__mark`'s flex-basis switches from the desktop rule's `24%` to a fixed `77px` at mobile, since percentage flex-basis needs a definite-width containing block, which the now content-sized lockup no longer provides. Hit a real flexbox bug along the way: `.brand-imagery__lockup { width: auto }` (the default) under-measured itself to roughly the mark's width alone, silently ignoring the wordmark, and let the text overflow past the lockup's own box below ~400px viewport width — `width: max-content` fixed it. Verified zero overflow and correct centering from 320px through 767px viewport width, and that the click/burst/regrow cycle still works at the new mobile mark size.
  - **Composition fix, round 3 (2026-09-22):** an annotated screenshot (arrows from the viewport edges to the ghosts and to "jimreed.net") showed the whole composition sitting well left-of-center at wide viewports — left margin (viewport edge → ghosts) 255px vs. right margin (jimreed.net's right edge → viewport edge) 360.75px at a 1590px-wide window. Jim asked specifically for the lockup (not the PacMan scene) to shift right ~80px. Added `transform: translateX(80px)` to `.brand-imagery__lockup`, scoped to `@media (min-width: 768px)` (mobile stacks the two vertically, so the side-by-side margin balance doesn't apply there). A `transform` was used deliberately instead of a margin or bigger flex `gap`, since either of those would have fed back into the row's `justify-content: center` math and nudged the PacMan ghosts too — a transform is paint-only and leaves layout/centering untouched. Result: 255px vs. 280.75px, much closer. Verified the click/burst cycle still hits correctly post-shift and that the mobile breakpoint's `transform` computes to `none`.

- [x] Fix WCAG AA text contrast on light surfaces — done 2026-09-21 (branch `fix/wcag-aa-contrast-issue`). Took the recommended "darken text only" option, so the image framing look is unchanged (`src/index.css`, `:root` tokens):
  - `--color-text-muted-light` `#888888` → `#6D6D6D`: **4.55:1** on `--color-surface-light` (was 3.12:1). Every remaining use is text sitting on the page background (`.intro__cue`, `.more-work__desc`, `.case-study__lead`, inline `figcaption`, `.case-study__meta-item dt`, `.case-study__nav-label`), so `#656565` (needed only on `--color-surface-mid`) wasn't necessary. No muted-light text sits on the Featured band.
  - New `--color-frame: #888888` for the non-text uses that should keep the old grey: the image / video-frame / Lightbox-image borders and the inactive carousel dots (3.12:1 clears WCAG 1.4.11's 3:1 for UI components on the page background).
  - New `--color-accent-on-light: #2F63D9` (**4.72:1** on `--color-surface-light`, was 3.48:1 with `#3D7AF5`) for accent used as text on light: `.nav__link` hover/active and `.case-study__nav-link:hover`. `--color-accent` itself is unchanged, since its other text uses are on dark surfaces where it passes (4.89:1 on near-black) or are decorative bullets.
  - Not changed, Jim is fine with it for now (2026-09-21): white 15px/500 label on the `--color-accent` fill in `.profile__btn` is **3.96:1** (needs 4.5:1). Fixing it means either a darker button fill (`#2F63D9` gives 5.37:1 but looks darker against the navy Profile panel) or larger/bolder text, both visible design changes.
  - Also noted, Jim is fine with it for now (2026-09-21): inactive carousel dots on the Featured band (`#E5E2DE`) are 2.75:1 with `#888888`, just under 3:1; left as-is pending a look.
  - Same session, at Jim's request: inline case study `figcaption` bumped from 14px to 15px. Developer Experience's two storyboard captions rewritten because they didn't make sense to a reader ("Elaine's own line drove the bet" → quotes her panel 5 line, "Listing APIs is nice, but I want to try the technology."; "The five-minute deploy claim came from this frame" → "Elaine's app is running in under five minutes (panel 13): a design target, not a measured result."). Neither is checked in the Lightbox yet (both ~97-99 characters, at the edge of the one-line budget).
  - Verified: ratios recomputed with a WCAG luminance script for every text/background pair; `npm run build` clean. Not yet eyeballed in the browser: the backlog asked for a visual review of the lead, captions and meta labels, since the darker grey loses some hierarchy against `#111111` body text.

- [x] Elevate the Lightbox caption's visual treatment — done 2026-09-21 (branch `fix/lightbox-captions-font`). `.lightbox__caption` (`src/index.css`) is now `--font-display` (Roboto Slab) at 20px / weight 700 / line-height 1.4, 16px on mobile (<768px), colour `--color-text-bright-on-dark` (white; changed at Jim's request from the muted `rgba(255,255,255,0.7)` the item originally specified), applied uniformly to every Lightbox caption; the inline `figcaption` is untouched. Decided with Jim: caption stays **below** the image, and the box widens from a fixed 640px to a fixed **1000px** (rather than rewriting captions or matching image width); two-line wraps are allowed at narrower viewports, with outliers flagged. Roboto Slab only loads 500 and 700 (`index.html`), so weight 600 was never an option; 700 was chosen over 500 because 500 barely reads apart from body text.
  - Measured across all 85 Lightbox captions (Playwright, every image on all 9 pages, checking line count and figure overflow against the viewport and counter): **all on one line at 1440x900 and 1280x720** (a 22px size wrapped 9-10 of them even at desktop, which is why 20px). 1024 wide: 64 one-line / 21 two-line. 768 wide: 84 two-line. 390 wide: 66 two-line / 19 three-line. No overflow past the viewport or into the counter at any size. So the ~90-100 character budget (principle 13) still holds at desktop and **no captions needed rewriting**.
  - Known deviation, on purpose: below roughly 1120px viewport width (what the widest caption, 96 characters at 964px of the 1000px box, needs) the Lightbox caption wraps to two lines (three on phones), which principle 13's "one line wherever it appears" doesn't cover; accepted since the rule was written for desktop, and the alternative was shrinking the type. Revisit only if the wraps look bad on real devices.

- [x] Refine case study content, first pass — the initial build seeds each page from jimreed.net with a Problem/Approach/Outcomes skeleton. Raise it to a senior/principal standard: reframe "Approach" around key decisions and tradeoffs (not a list of process activities), surface a one-line outcome in the hero, make individual contribution explicit in the prose, give captions design rationale rather than labels, add a "Reflection / what I'd do differently" section, and replace all `[bracketed]` placeholders (timelines, outcome metrics, team) with real content.
  - **First pass complete (2026-09-18).** All 9 listed case studies have had their full refine pass (source review, lead-as-outcome, decision-tier headings, Reflection, one-line captions, images re-exported). A second pass is planned, combined with the image work into one "Case study second pass" item in Backlog; the further pass on the first 4 pages flagged on 2026-09-14 below is now part of it.
  - Provenance (2026-09-09), so the refine pass knows what it's working from: the featured 3 (agentic-ai-chat, genomic-data-platform, ai-recommendations) were built from Jim's own writing in earlier sessions (PRs #14, #19, #20) and are the model the rest was shaped toward. The 6 non-featured "more" pages were restructured that same session from a 4-line skeleton into the `blocks` format, with prose that's Claude's synthesis of thin myportfolio text plus production-image detail, not yet reconciled against the source, live on the site as Jim's real history (a genuine credibility risk until that reconciliation happens). The 3 unlisted case studies (samsung, banking, walmart) carried the most invented connective tissue and were removed entirely 2026-09-11 rather than rehabbed.
  - Copy style (2026-09-09): no em-dashes anywhere (description, blurb, body, alt text, role) per Jim's preference. Use commas, colons, parentheses, or separate sentences.
  - **Working notes live in `planning/refine-case-studies.md`**: the cross-cutting framework (13 principles as of 2026-09-14, covering lead-as-outcome, heading tiers, reflection sections, captions, source review, image treatment, one-line captions) plus per-page notes for every remaining case study.
  - **One-line captions (2026-09-14, principle 13): every caption on the 4 done case studies rewritten to render on one line, no wrap, inline or in the Lightbox.** The Lightbox's fixed 640px caption width is the usual binding constraint (narrower than most images' inline display width), except images with `maxWidth` set below 640, which bind inline first. 37 captions across Operational Insights, Agentic AI Chat, Genomic Data Platform, and AI Recommendations rewritten and verified by measuring actual rendered `scrollHeight` in a headless browser (both contexts), not by eye or character count. Also dropped a redundant bulleted list in Agentic AI Chat's "What SREs Need Mid-Incident" (duplicated the `user-needs.webp` image right below it). Now a standing content principle: apply it to the remaining 5 pages during their refine pass, and to any caption touched on the 4 already-done pages going forward.
  - Pilot complete (2026-09-11/12): Operational Insights, chosen as the strongest of the 6 "more" pages, was fully rewritten and all 9 of its images re-exported to WebP (opaque, bordered/rounded, dimension-tracked) per the "Case study image assets" decisions below. Framework validated.
  - **Current priority, decided 2026-09-12: the featured 3 are next**, ahead of the remaining "more" pages. They're the highest-traffic pages on the site (what most visitors click first) and the lowest-effort remaining work, targeted editing against an existing punch list rather than the reconciliation-against-thin-source work the "more" pages still need. After the featured 3: the remaining 5 "more" pages, still carrying the credibility-risk concern noted above, roughly in this order: Developer Experience first (real outcome arc already, closest to done, was the earlier "next" pick before the featured 3 jumped ahead of the whole batch), then Machine Learning for Operators, Mobile Emergency System, Additive Manufacturing, and Analytics & Big Data, each with a bigger gap to close (see `planning/refine-case-studies.md` for specifics). Full source review per principle 10 (myportfolio original + complete `production images/` folder) is the first step for whichever page comes next; done now for 2 of the featured 3, not yet for AI Recommendations.
  - **Agentic AI Chat done (2026-09-13)**, first of the featured 3 — branch: fix/refine-case-study-agentic-ai-chat. Source review found all 6 production images already wired in (nothing missing, unlike the Operational Insights pilot), plus one new detail worth surfacing: an early concept explored a separate name for the Slack bot, O11y Copilot, before the team settled on one name, Observe Agent, everywhere (corrected after an initial misreading had both names shipping); now the close of the Branding section, evidence of the icon exploration feeding a naming decision. Reordered to lead with the SRE persona (User Needs moved from position 4 to 2, renamed "What SREs Need Mid-Incident"); UI Principles and Elements and Interactions reframed around their actual decisions (side panel over a takeover; inline artifacts over linking out); added a Reflection section; standardized the metric on "10,000+ engineers" everywhere; normalized the schema from legacy `body`/`image` to `blocks`. Images: all 6 re-exported to WebP (2026-09-13, same session; final versions re-exported by Jim himself from the real source files, superseding an earlier programmatic patch job), renamed to descriptive ids, dimensions updated to match; `interaction-specs` turned out to be a genuine alpha composite and is flagged `unframed`. Folds this case study's share of the "Case study image assets" item above into Done too. Full details in `planning/refine-case-studies.md`. Verified with `npm run build` (clean) and Playwright screenshots (desktop, mobile, and the Lightbox) throughout.
  - **Genomic Data Platform done (2026-09-14)**, second of the featured 3 — branch: fix/refine-case-study-genomic-data-platform. Source review of the full `production images/Illumina/` folder (29 files, not just the 12 already wired in) found two unused images: a design-principles slide (left out, too disconnected from the narrative) and a Marker Frequency comparison view that turned out to be a closer match than the already-wired heat map to a direct customer quote already in the case study (William LaFramboise, Allegheny Health Network, asking to compare gene variance across cohorts); both features now shown as the shipped answer to that call. Collapsed the five-section methods tour (Personas, Card Sorting, User Interviews, Storyboards, Prototypes) to two: "One Persona to Design Around" and "Cohorts That Nest, Compare, and Save," the latter naming specifics (temporary tabs, 1000 Genomes ethnicity estimation, save/share snapshots) read directly off the storyboard's 15 frames, previously illegible in the banner-cropped export. Pulled the customer-feedback and user-testing sections forward; expanded Cross-Product Design into "One System Across Three Very Different Data Types," corrected to claim only what the screenshots actually show carries over (not identical page chrome). Added a Reflection section. Images: all 13 (12 re-exported + the newly surfaced Marker Frequency view) first converted to WebP via Pillow (no cwebp/ImageMagick in this environment), then 8 re-exported again by Jim himself from the real source files, superseding the Pillow pass for those with tighter crops; 8 swapped to their unbannered `-v1` source per principle 7; `personas` and `storyboards` flagged `unframed` for a genuine soft-shadow "stacked paper" effect baked into the source graphic. Deleted an orphaned, never-referenced `01.png` found sitting in the folder since the original #12 PR. Folds this case study's share of the "Case study image assets" item into Done too. Full details in `planning/refine-case-studies.md`. Verified with `npm run build` (clean) and Playwright screenshots (desktop, mobile, and the Lightbox, both `unframed` images included).
  - **AI Recommendations done (2026-09-14)**, last of the featured 3 — branch: fix/refine-case-study-ai-recommendations. **All three featured case studies are now done**, pending review and merge. Source review of the full `production images/AI Recommendations/` folder (27 files, not just the 13 already wired in) turned up a misfiled GE Predix asset (excluded, not part of this case study) and two corrections against the shipped artifacts: the persona the prose called "Citizen Integrator" is labeled "Citizen Developer" in the actual shipped design, and "Visual Redesign" claimed a before-and-after that the source image never showed. Restructured 11 sections into 8. The initial punch list marked "Sketches and Renderings" and "Storyboards" as one-sentence throwaways, written before the source review; un-cropped, the storyboard turned out to be a fully legible 8-frame sequence and the concept renderings showed the actual design bet directly (a recommendation as a dashed placeholder inline in the canvas, not a separate panel). That image now anchors a new decision-tier section, "Inline, Not a Separate Panel," built around it and the predictive-text analogy; the rest of the evolution material consolidated into one supporting section instead of four thin ones. Cut the standalone "Customer Delight" section, moving its attributed quote (Keri-Ann Bowen, ETL Developer, University of Maryland, found in the source image but stripped from the alt text) into the precision-model section as evidence. Added a Reflection section. Images: all 12 in-narrative images plus the hero converted to WebP via Pillow (no cwebp/ImageMagick in this environment), banners cropped off in favor of HTML captions, preferring each `-v1` unbannered sibling per principle 7; the one image with no `-v1` (`Informatica-brand.png`) had its banner sitting over a redundant thumbnail column, cropped out entirely. The animated GIF converted to an animated WebP rather than deferred to the separate video-block backlog item, since it's a short looping UI prototype, not the longer-form Vimeo content that item is scoped around. As with Genomic Data Platform, this was a Pillow-only pass; Jim then re-exported several himself (hero, storyboard, visual-redesign) with tighter crops, superseding the Pillow versions for those; a couple of images (predictive-text analogy, precision model) were narrowed with `maxWidth` afterward since they read better smaller. Folds this case study's share of the "Case study image assets" item into Done too. Full details in `planning/refine-case-studies.md`. Verified with `npm run build` (clean) and Playwright screenshots (desktop, mobile, and the Lightbox, including the animated prototype frame).
  - **Interim fix (2026-09-14): all images in the 5 not-yet-refined "more" pages (Developer Experience, Machine Learning for Operators, Mobile Emergency System, Additive Manufacturing, Analytics & Big Data) flagged `unframed`.** Not a content refine, a presentation-layer stopgap: those pages' source images still carry their old baked-in rounded corners and drop shadows (never re-exported to the new opaque/no-baked-chrome convention), and the site-wide `.case-study__media img` border + radius CSS was stacking a second frame on top, a visibly doubled border/corner. `unframed` opts them out of that CSS so each page reads as one clean frame instead of two, without touching any prose or requiring the images themselves to be touched. This is provisional: when each of these 5 gets its full refine pass, its images should get the real treatment (re-exported opaque, banners cropped to captions, framed normally, `unframed` removed unless a genuine alpha composite turns up), matching the featured 3 and Operational Insights. **Priority after this: further refining the 4 case studies already done (Operational Insights, Agentic AI Chat, Genomic Data Platform, AI Recommendations)** before moving on to the remaining 5. **Update (2026-09-18): all 5 have now had their full refine pass and their images re-exported, so this stopgap is fully cleared: none of the 5 pages carries the blanket `unframed` flag any more, only per-image ones (checked against `caseStudies.json` 2026-09-18).**
  - **Developer Experience done (2026-09-16)**, first of the remaining 5 "more" pages, branch: fix/refine-case-study-developer-experience. Source review of the full `production images/Predix/` folder (18 files, not just the 6 already wired in) found two real unused assets: a shot list for the coding-tip video series (title card, medium close, over-the-shoulder, screen capture shots) and a screenshot of the actual shipped Catalog page (eight services live across two categories), both now wired in as new evidence for the "Driving Developer Adoption" and "From Design to Product Management" sections. "A Persona to Design Around" retitled "One Persona, Not a Committee of Them" and reframed as a decision (one persona vs. a roster of them). Added a Reflection section (the five-minute deploy target was a bet never checked against real usage data; moving into the PM seat blurred advocating for developer experience vs. trading it off against the roadmap). This case study's share of the "Case study image assets" item (below) also done, clearing its 2026-09-14 interim `unframed` flag. Full details in `planning/refine-case-studies.md`. Verified with `npm run build` (clean) and Playwright screenshots (desktop, mobile, and the Lightbox).
  - **Machine Learning for Operators done (2026-09-16)**, second of the remaining 5 "more" pages, branch: fix/refine-case-study-machine-learning-for-operators. Source review of the full `production images/AI Workbench/` folder (15 files, not just the 5 already wired in) found three real unused assets: a "CASE STUDY" slide stating the actual business stakes (a power plant manager bidding into the electricity market, penalized for underproducing, wasting fuel for overproducing) that the page never stated anywhere, now its own new section, "Bidding Into the Market, One Percent at a Time"; a conceptual map of the roles (Data Engineer, Data Scientist, Analytic Developer) and catalogs (Marketplace, Data Catalog, Model Builder, Analytics Catalog) a trained model moves through, matching a one-line mention in the myportfolio original that had never been illustrated; and a correlation-coefficient matrix and density heat map cropped from a larger data-visualization screenshot, a real data-exploration feature never shown before. "Seeing the Data Before Training" and "A Guided Model-Building Flow" (renamed "A Wizard That Trades Flexibility for Guardrails") promoted to the two decision-tier headings per the punch list, each gaining a new-find image. No adoption number existed to state or invent (the screens' own numbers, like the 69.91% accuracy readout, are demo content); the qualitative outcome is the shipped capability itself, a guided wizard inside GE's Predix-powered AI Workbench. Added a Reflection section relating the classic UX toolkit to this AI-era process (an engineer's own data-curve sketch stood in for a storyboard) plus two open questions on trust and legibility (a single accuracy percentage, a correlation matrix assumed to be self-teaching). This case study's share of the "Case study image assets" item (below) also done, clearing its 2026-09-14 interim `unframed` flag. Full details in `planning/refine-case-studies.md`. Verified with `npm run build` (clean) and Playwright screenshots (desktop, mobile, and the Lightbox), Playwright installed fresh into the scratchpad directory since neither it nor a system Chromium was preinstalled in this environment.
  - **Mobile Emergency System done (2026-09-16)**, third of the remaining 5 "more" pages, branch: fix/refine-case-study-mobile-emergency-system. Source review of the full `production images/AtHoc/` folder (15 files, not just the 8 already wired in) found two real unused assets: a full-size, unbannered close-up of the emergency-message composer (text, a dropped pin, an attached photo), previously only visible cropped small inside the hero; and a UI pattern-library sheet (buttons, fields, popovers, contextual help), real evidence behind the existing "One System, Every Screen" claim, never wired in. Also confirmed the myportfolio original's "Motion Study" callout (an animated UI study) doesn't match the shipped `motion-study.png`, a static flow diagram, matching an existing flag in the "Restore case study video content" item below; renamed the id to `screen-flows` so the page stops implying a video that isn't there. Recovered a real role from the myportfolio byline, Principal Product Designer, BlackBerry AtHoc, not previously in `caseStudies.json`. Consolidated the four process-heavy craft sections (Rapid Sketches, Screen Flows, Lo-Fi Wireframes, Mood Board) to two per the punch list: the Mood Board promoted to a decision-tier lead ("Emergency Controls Borrowed From the Hardware on the Wall," the skeuomorphic red-control bet), the other three merged into "Ten Screen Templates, Then Every Path Between Them." No adoption or deployment-scale number existed to state or invent; the outcome folded into the lead is qualitative (shipped across iOS, Android, and desktop). Added a Reflection section on two untested risks visible in the artifacts themselves: the always-on-screen EMERGENCY button's accidental-trigger risk, and whether the failed-send Retry control's premise (a calm person who taps to retry) held up under real high-stress use. This case study's share of the "Case study image assets" item (below) also done, clearing its 2026-09-14 interim `unframed` flag. Three images (`mood-board`, `screen-flows`, `wireframes`) had banners this automated pass couldn't safely remove, each with no unbannered sibling and a banner sitting over real content (a hidden photo tile, a diagram, an overlapped status bar) where patching would mean fabricating pixels; Jim resolved all three himself by re-exporting from the real source files (`mood-board` 2026-09-16, `screen-flows` and `wireframes` 2026-09-17), each superseding its Pillow-pass version with the banner cleanly gone. `mood-board` and `screen-flows` came back fully opaque and dropped `unframed`; `wireframes` kept its genuine soft-shadow composite and stays `unframed`. Zero banners remain baked into any image on this page. Jim also re-exported `tablet` (2026-09-17) keeping its device bezel's real transparent rounded corners intact rather than the Pillow pass's flattened-opaque version, so it went back to `unframed` (matching the `catalog-console-wireframes` precedent on Developer Experience) to avoid the site's standard border boxing a square frame around an already-rounded silhouette. Full details in `planning/refine-case-studies.md`. Verified with `npm run build` (clean) and a Playwright script (installed fresh into the scratchpad directory) confirming no console errors and all 10 captions rendering on one line, inline and in the Lightbox.
  - **Additive Manufacturing done (2026-09-17)**, fourth of the remaining 5 "more" pages, branch: fix/refine-case-study-additive-manufacturing. Source review of the full `production images/Additive/` folder (19 files, not just the 7 already wired in) found three real unused assets: `additive-progress-donut-v1.png`, the full shipped Availability Overview dashboard showing the OEE donut grid right next to a printer's own progress donut (same ring shape, "PRINTING 48%"), the clearest possible evidence for the donut-confusion story; `Additive-mobile.png`, a side-by-side of the hand-sketched phone concept next to a mockup of the shipped color-coded Builds list, a real sketch-to-shipped pairing the previously-wired tighter crop obscured; and an unbannered persona sibling that names the Operator persona **Stacey**, never surfaced before. "Rethinking the Visualization" promoted and retitled "Why the Donut Chart Had to Go," now the lead section, arguing the decision from two real numbers read off the screenshots (an 85% OEE ring, a printer at 48% printed) rather than an abstract description. "Early Concept Sketches," "A Journey Map of Roles," and "Personas: Goals and Challenges" collapsed into one section per the punch list, "Sketches, a Journey Map, and a Persona Named Stacey." Real metric added per the punch list's "facility rollout" ask: the Availability Score shipped across real GE Additive site names visible in the screenshots, folded into the lead and a closing "One Score, From Lichtenfels to Shanghai" section rather than the old bare "Outcome" label. New section "Watching Builds Get Tracked by Hand" (was "Observing Users On Site") surfaces a real finding legible only once the site-visit photo was unbanned and full-size: engineers tracking builds by hand on a whiteboard, engineer names, phone numbers, and estimated completion written and erased as jobs moved. Added a Reflection section built from the donut misfire and the whiteboard finding already in the page: whether testing the ring against the printer's own UI earlier would have caught the conflict before a design round was spent on it, and whether the console ever replaced the whiteboard's ownership/ETA annotation. This case study's share of the "Case study image assets" item (below) also done, clearing its 2026-09-14 interim `unframed` flag: 7 of 9 images dropped `unframed` (confirmed under 2% alpha each, just corner-rounding), one genuine composite (`persona-stacey`, a stacked-paper shadow) stays `unframed`. `concept-sketch` initially kept its baked-in banner as a documented exception (it sat over photographed content, a shoulder, not diagram content, with no unbannered sibling to swap to); Jim resolved it himself the same day by re-exporting from the real source file, a tighter 720×510 crop with the banner cleanly gone, so zero banners remain baked into any image on this page. Full details in `planning/refine-case-studies.md`. Verified with `npm run build` (clean) and a Playwright script (installed fresh into the scratchpad directory) confirming no console errors and all 9 captions rendering on one line, inline and in the Lightbox.
  - **Follow-up (2026-09-17, same day):** the myportfolio original described a fleet-wide dashboard drilling down to individual printers, missed on the first source review; Jim supplied a new fleet screenshot and asked for that topic restored ahead of the donut story, then, after a couple of rounds landing on the right structure, confirmed it's a three-level story: fleet, facility, and individual printer, using the new fleet shot as the hero, the pre-existing hero image (a Lichtenfels Builds table, actually the facility level) as the middle step, and the other new screenshot (one machine's current job) as the bottom. Files renamed to match: `dashboard-fleet.webp` (hero), `dashboard-facility.webp` (was `dashboard.webp`), `dashboard-machine.webp` (was `dashboard-location.webp`). New section "Drilling From the Fleet Down to One Job" added right after the intro, all three levels in one paragraph, two images (fleet is the hero, shown once). The fleet screenshot's facility list (Lichtenfels, Grapevine, Shanghai, and six more, real and consistently labeled) also turned out more authoritative than the OEE-donut screenshot's, so the lead and the closing "One Score" section were updated to cite it instead, replacing an earlier, shakier "four continents" framing. Re-verified with `npm run build` and Playwright: all 12 captions render on one line, inline and in the Lightbox.
  - **Follow-up (2026-09-18):** added `team` metadata ("Another visual designer, with user discovery done alongside the lead engineer and architect") after Jim asked whether his own role should be called out; landed on leaving the site-wide convention as-is (`team` describes collaborators, `role` above it already covers Jim's title) but confirmed via a quick question that Jim was hands-on in visual design too, not just interaction design, hence "another." Also capped `concept-sketch` at `maxWidth: 480` (native 720×510) since it reads better narrower alongside the journey map and persona card in the same section.
  - **Analytics & Big Data done (2026-09-18)**, fifth and last of the remaining 5 "more" pages, branch: fix/refine-case-study-analytics-big-data. **All 9 case studies have now had their full refine pass.** Source review of the full `production images/GE Analytics/` folder (13 files, not just the 5 already wired in) plus the wider `case studies/GE-Analytics/` working folder found two real unused assets: a six-frame storyboard of the Orchestration Editor's interaction (components dragged onto a canvas, connections turning from red to green as fields are dragged onto an analytic's inputs), and the unbannered paper-wireframing photo. Also caught drift in the draft against the myportfolio original: the intro had lost "interviewed dozens of data scientists and data engineers" to a vaguer "research," and added a "no tool made it easy" clause that appears nowhere in the source, both corrected. Restructured 5 sections into 3 plus a Reflection: "One Visualizer, Before and After the Analytic" (decision tier, builds on "I conceived Data Explorer": the comp's breadcrumb and the task flow's shared Data Visualizer both show it sitting at both ends of an analytic run, not just as a start-only cleanup step), "Red Until Configured, Green When Ready" (decision tier, the Orchestration Editor's connection-status-on-the-arrow interaction, with the hand sketches as continuity evidence), and "A Sticky-Note Wall That Became the Five-Step Task Flow" (supporting: the wall's notes are already in Upload, Pick, Config, Execute, Evaluate columns). Reflection built from what the artifacts show: color alone carries the configuration state, and Data Explorer's shared-visualizer bet was never tested against real use since it was never prioritized. **Shipped vs. concept, first left open, then resolved by Jim the same day:** the Orchestration Editor shipped with a companion product, the Analytics Catalog (a lane in the task flow), and Data Explorer was well received by product management and executive leaders but never prioritized for development. Lead, blurb, both product sections, the hero caption ("Data Explorer concept"), and the second Reflection paragraph were rewritten to say so; no metric was given or invented. Also flagged: a possibly-relevant workshop room photo (`GE-SR0012.JPG`, not tied to this project by anything in the source) and three different product names across the artifacts (Analytic Application Workbench, Predix Insight, Analytic Cloud), see `planning/refine-case-studies.md`. Images: 7 (up from 5), all re-exported to WebP via Pillow (~2.8MB of PNG down to ~440KB), clearing the 2026-09-14 interim `unframed` stopgap: the two browser-chrome comps flattened to opaque and framed, both photos from their unbannered `-v1` siblings, three genuine stacked-paper composites (`task-flows`, `storyboard`, `sketches`) keep alpha and stay `unframed`. Jim then re-exported `data-explorer` and `orchestration-editor` himself (1000×718 each, superseding the Pillow pass; effectively opaque, still framed normally) and `sketches` (934×708, still `unframed`, capped at `maxWidth: 640`). 100%-scale, not 2x, since the curated PNGs are all under ~1140px; Jim can re-export from the working PSDs for a 2x pass. Old PNGs deleted; `width`/`height` recorded for all 7. Verified with `npm run build` (clean) and a Playwright script (installed fresh into the scratchpad directory) confirming no console errors and all 7 captions rendering on one line, inline and in the Lightbox.

- [x] Case study image assets, first pass: re-export every case-study image, one at a time per remaining case study, following the pattern validated on Operational Insights (2026-09-11/12, all 9 of its images done): WebP, opaque (no alpha unless it's a genuine composite, then flag `unframed`), no banners baked in (write an HTML `caption` instead, see the refine-case-studies.md framework), no rounded corners baked in (CSS handles it globally now), native/100% scale (no upscaling yet), record final `width`/`height` in `caseStudies.json` as each one is re-exported. Images keep their own varied native sizes (resolved 2026-09-12: option (b), not one standard width like 1600px); use the numeric `maxWidth` field on an image block when a specific image reads better narrower.
  - **First pass complete (2026-09-18).** All 9 listed case studies' in-page images are re-exported (Analytics & Big Data, the last, closed the 2026-09-14 interim `unframed` stopgap for the 5 "more" pages). Jim wants a second pass, folded into the "Case study second pass" item in Backlog (content refine and image re-export together), see it for what carries over, mainly bumping the remaining 100%-scale exports to the 2x standard.
  - Agentic AI Chat's 6 images done (2026-09-13, folded into the "Refine case study content" pass on branch fix/refine-case-study-agentic-ai-chat). None had an unbannered `-v1` source to swap to like some of Operational Insights' did; a first pass patched the banners out programmatically, but Jim then re-exported all 6 himself from the real source files, superseding that patch job with cleaner results. One of them, `interaction-specs`, turned out to be a genuine alpha composite (a drop-shadowed stack of screenshots, not a flat rectangle) and is flagged `unframed`. Final assets: WebP, renamed to descriptive ids matching the Operational Insights convention, `width`/`height` updated to each re-export's actual dimensions. See `planning/refine-case-studies.md` for the full account.
  - Genomic Data Platform's 13 images done (2026-09-14, folded into the "Refine case study content" pass on branch fix/refine-case-study-genomic-data-platform). 8 of the 12 previously-wired images had an unbannered `-v1` source (like Operational Insights, unlike Agentic AI Chat); those became the new base images, with removed banner text seeding several captions. First pass converted to WebP with Pillow (no cwebp/ImageMagick available in this environment); Jim then re-exported 8 of the 13 himself from the real source files (`survival-comparison`, `card-sort`, `user-interviews`, `gene-heatmap`, `marker-frequency-comparison`, `radar-charts`, `proteomics`, `spatial`), superseding the Pillow pass for those with tighter crops and smaller file sizes, each checked and its `width`/`height` updated to match. The remaining 5 (`personas`, `storyboards`, `prototype`, `customer-feedback`, `user-study`) stayed on the Pillow export. Two, `personas` and `storyboards`, have a genuine soft-shadow "stacked paper" effect baked into the source graphic (not a hard rectangle) and are flagged `unframed`, extending the precedent beyond Mobile Emergency System's device composite. A 13th image, the previously-unused Marker Frequency comparison view, was added after the source review found it sitting unwired in the folder. Also deleted an orphaned `01.png` never referenced anywhere, present since the original #12 PR. See `planning/refine-case-studies.md` for the full account.
  - Developer Experience's 8 images done (2026-09-16, folded into the "Refine case study content" pass on branch fix/refine-case-study-developer-experience), clearing the 2026-09-14 interim `unframed` stopgap for this page. 5 of the 6 previously-wired images plus the conference photo had an unbannered `-v1` source; those became the new base images, removed banner text seeding several captions. Converted to WebP via Pillow (no cwebp/ImageMagick in this environment); flattened to opaque where the alpha channel was just rounded-corner anti-aliasing on browser-chrome mockups (home, wireframes, community), not a real composite. Two, `persona-storyboard` and `five-minute-storyboard`, have a genuine soft-shadow "stacked paper" alpha composite (irregular torn-edge card outline) and are flagged `unframed`, matching the Genomic Data Platform precedent; the other 6 are opaque and framed. Two previously-unused source images surfaced by the source review were added as new ids: `video-shot-list` (a real shot list for the coding-tip video series) and `shipped-catalog` (the actual shipped Catalog page, evidence for a real catalog-size number). Renamed to descriptive ids, `width`/`height` recorded for all 8, old PNGs deleted. Jim then re-exported `hero.webp` (849×720, tighter crop), `catalog-console-wireframes.webp` (1014×582), and `community-video.webp` (849×880) himself from the real source files, superseding the Pillow pass for all three; the wireframes re-export kept its real transparent panel gutter intact, so it's now flagged `unframed` too, joining the two storyboard images as this page's alpha composites; the community re-export swapped to the final shipped "Eager to Innovate?" CTA state rather than the `-v1` sibling's earlier copy this pass had picked. See `planning/refine-case-studies.md` for the full account.
  - Machine Learning for Operators' 8 images done (2026-09-16, folded into the "Refine case study content" pass on branch fix/refine-case-study-machine-learning-for-operators), clearing the 2026-09-14 interim `unframed` stopgap for this page. 4 of the 5 previously-wired images had a confirmed (pixel-compared, not assumed) unbannered `-v1` sibling with identical content; the hero swapped instead to a different, unbannered, lower-scroll crop of the same dashboard that turned out to be a strict superset once combined with the original's top half, no patching needed after a two-band pixel-fill patch attempt on the original banner was tried first and superseded. Converted to WebP via Pillow (no cwebp/ImageMagick in this environment); flattened to opaque where the alpha channel was just mockup-frame corner rounding (confirmed under 1.3% alpha on each, checked directly rather than assumed). One genuine composite: `product-ecosystem.webp` (a conceptual role/catalog map, a new find from the source review) has an irregular torn-edge stacked-paper shadow baked into the source graphic, confirmed by inspecting its alpha channel, and is flagged `unframed`, matching the Genomic Data Platform and Developer Experience precedent; its own banner had no unbannered sibling and was patched out with a flat white fill instead (confirmed safe: it covers blank paper, not diagram content). Two more previously-unused source images surfaced by the source review were added as new ids: `market-stakes` (a "CASE STUDY" framing slide stating the real business stakes, bidding into the electricity market, that the shipped page never stated anywhere) and `correlation-plot` (cropped from a larger data-visualization screenshot, a correlation-coefficient matrix and density heat map, a real data-exploration feature never shown before). Renamed to descriptive ids, `width`/`height` recorded for all 8, old PNGs deleted. Total asset weight dropped from ~1.5MB of PNGs to ~600KB of WebP across one more image than before (8 vs. 5). See `planning/refine-case-studies.md` for the full account.
  - QA found (2026-09-04): same asset set has a performance angle worth folding into this pass — `public/assets/case-studies/` is ~7MB of uncompressed PNGs (several 150–840KB each), and none of the `<img>` tags carry `width`/`height`/`aspect-ratio`, so they cause layout shift as they lazy-load in. Since any re-export touches these same files, do it once: pick the standard width, export as compressed WebP, and record each image's dimensions (or a fixed aspect-ratio per placement) so the markup can reserve space.
  - Added 2026-09-08 (More Case Studies rebuild): ~45 section images + 9 homepage thumbnails for the 9 non-featured case studies, copied verbatim from `../case studies/production images/<Folder>/` as the original PNGs — most carry an alpha channel on purpose so they read on any background, so any re-export must stay lossless-with-alpha (WebP is fine; JPEG is not). Native widths run ~600–1140px; no `width`/`height` attrs. The homepage thumbnails point at the full `*_cover*` PNG and are cropped to 3:2 by CSS `object-fit: cover`. Fold into the same standardized re-export + dimension-reservation pass. Source→dest mapping is in this branch's scratch notes (`copyorig.sh`).
  - Note: the 6 listed "more" homepage thumbnails were already re-exported as curated 1200×800 WebP (branch fix/curate-more-case-studies-thumbnails, PR #28) — those are done. This item is the in-page section images plus the featured-carousel images.
  - Decided 2026-09-11 (via the "Refine case study content" item): annotation banners baked into image pixels move to HTML captions, rendered inline and in the Lightbox. Piloted on `operational-insights`'s 7 images: where the raw `production images/` source had an earlier, less-annotated `-v1` sibling, that became the new base image (a plain top-strip crop removed a residual plain-text title on two of them); the removed banner text seeded each new caption. This standardized-re-export item still owns the full pass (WebP, uniform width, dimension attrs) across every case study's images, including these swapped-in ones.
  - Decided 2026-09-11/12: every case-study image also gets a standard 1px border + 6px radius by default (`.case-study__media img` in `src/index.css`, radius matched to the corner already baked into the mockup screenshots themselves), with a per-image `unframed` opt-out for alpha-transparent composites where a border would frame empty canvas instead of content. Because banners are being cropped/re-exported away and rounding is now a CSS treatment rather than baked into pixels, Jim expects most re-exports to drop the alpha channel entirely (no longer needed once there's no transparency to preserve for background-agnostic display) — updates and supersedes the 2026-09-08 note above that "any re-export must stay lossless-with-alpha." The documented exception: Mobile Emergency System's three-phone hero composite (`app-comps.png`) stays alpha-transparent and is flagged `unframed` in `caseStudies.json`. Any other case study's images that turn out to need the same treatment (composites, not single rectangular screenshots) should get the same `unframed` flag as they're re-exported.
  - Decided 2026-09-12, ahead of the first test re-export: **(a) vs (b) from the original item description resolved as (b)** — images keep their own varied native sizes, no convergence to one standard width like 1600px. ~~**2x/retina scaling deferred**: the first pass of PRs exports at 100% (no upscaling) rather than a flat 200%, since upscaling an image beyond the pixels actually captured doesn't add real sharpness (see the caveat below); revisit 2x as a separate decision once there's a plan for getting genuinely higher-resolution source captures, not just interpolating the existing PNGs larger.~~ **Superseded 2026-09-16: 2x is now the standard.** Jim has genuinely higher-resolution source captures to export from (not upscaling existing PNGs, the original blocker above), so new re-exports target 2x of the image's actual display width, not 100%. Concretely: an unconstrained section image displays at up to 872px on desktop (the bleed column width, see `planning/refine-case-studies.md` principle 7), so its 2x export is 1744px wide; an image with `maxWidth: N` set exports at `2 * N`. Existing already-shipped images (Operational Insights, the featured 3, Developer Experience, Machine Learning for Operators) stay at their current 100%-scale exports and get bumped to 2x opportunistically whenever that page is next touched, not as a dedicated retrofit pass. **Dimension tracking is now live**: `width`/`height` fields on the `hero` object and on each `image` block in `caseStudies.json`, rendered as HTML `width`/`height` attributes by `CaseStudyPage.jsx` (browsers derive the aspect ratio from these even with the existing responsive `max-width:100%;height:auto` CSS, so this doesn't change how anything displays, it only reserves the right space before the image loads; a 2x export's `width`/`height` should still record its actual pixel dimensions, e.g. 1744, not the CSS display size). Populated for all of Operational Insights' current images as the first case; carry this forward for every image as it's re-exported for the remaining case studies, so this doesn't become a separate pass later.

- [x] Bump `actions/checkout` and `actions/setup-node` to their latest major versions in `deploy-production.yml` and `deploy-staging.yml` — branch: chore/bump-deploy-workflow-actions
  - Both workflows bumped `actions/checkout@v4` → `@v5` and `actions/setup-node@v4` → `@v5`. Left `node-version: '20.x'` as-is per the note (build is known-good on 20, raising to 22.x was optional/low urgency and out of scope for this trivial bump).

- [x] Audit and standardize the text-link arrow affordances site-wide — branch: fix/adjust-linkedIn-links (folded in, no code changes needed)
  - Verified all three parts of the rule were already true, or became true as a side effect of the LinkedIn item above, so nothing needed changing:
    - **Convention** (backward/return/upward leads with the arrow, forward/onward trails it): holds for every current instance, no stragglers. Re-grepped for any arrow glyph in `src/` to confirm no pair was missed (the only other match, "Led 0→1 design" in `Profile.jsx`, is prose, not a link affordance).
    - **Glyph reconciliation** (U+2197 for external/leaves-the-site links, U+2192 for in-site forward links): done as part of the LinkedIn item — Profile CTA and the new footer LinkedIn link now match the nav's U+2197; Next/View Case Study stay U+2192.
    - **Spacing / orphan-arrow risk**: all seven label+arrow pairs already use a single plain space (nothing to reconcile there). Screenshotted every one (Back to all work, Previous/Next, View Case Study ×2, LinkedIn ×3, Back to Top) at 320px width (iPhone SE, narrowest realistic viewport) — none wrap, the arrow never separates from its word. `&nbsp;` would guard against a problem that doesn't occur in practice, so skipped.

- [x] Unify the LinkedIn links and add one to the footer — branch: fix/adjust-linkedIn-links
  - All three LinkedIn links now trail with the same glyph, U+2197 (`↗`): Nav was already correct. `Profile.jsx` CTA swapped its trailing U+2192 (`→`) to U+2197, label and button styling unchanged. Added a new `LinkedIn ↗` link to `Footer.jsx` (same href, `target="_blank" rel="noreferrer"`, `.footer__link` class), placed between the copyright and "Back to Top" in `.footer__row` (existing `space-between` flex row, no CSS changes needed — reads left-to-right as copyright / LinkedIn / Back to Top and doesn't wrap even at 390px).
  - Verified via `npm run build` (clean) and Playwright screenshots of nav, Profile, and footer at desktop (1280px) and mobile (390px) widths.
  - Caveat (accepted, unchanged from original note): U+2197 isn't in Roboto so it renders from a system-fallback font, slightly heavier than the surrounding text. Consistent across all three links regardless; revisit separately if it bothers.

- [x] Adjust CTA buttons and padding — a prominence + density pass on the case study pages, mostly CSS — branch: fix/adjust-cta-buttons-and-padding
  - "Back to all work" and "View Case Study" CTAs restyled to match Profile's "Connect on LinkedIn" button (`.profile__btn`): solid pill, `--color-surface-dark` background instead of `--color-accent`, white text, bounce/shadow on hover. Existing arrow placement kept (leading `←` on Back, trailing `→` on View Case Study). Label alternatives ("Back to Home", "Back to Work", "Back") evaluated with Jim; kept "Back to all work". `.case-study__back--center` switched from `display: block` to `display: table` so the now button-shaped link centers correctly instead of stretching full-width.
  - More Case Studies thumbnail CTA (`.more-work__thumb-cta`) font size bumped 13px → 15px to match the Featured CTA label; kept its existing hover-reveal-on-thumbnail pattern rather than converging to button chrome.
  - Top padding on `.case-study__inner` reduced: 120px → 72px desktop, 88px → 48px mobile (`src/index.css:933`). Bottom padding, `.section__inner`, and `.case-study__hero` margin-bottom left as-is — still open if the "too airy" feeling should be addressed elsewhere.
  - Also fixed a pre-existing CSS lint warning on `.more-work__desc` by adding the standard `line-clamp` property alongside `-webkit-line-clamp`.

- [x] Homepage <> case-study navigation: hard cut, no scroll animation — branch: fix/homepage-case-study-transitions
  - Started as "explore a directional slide transition" (the idea: into a case study slides in from the right, back to the homepage slides in from the left, like turning your head). Built a full dependency-free version — a `RouteTransition` wrapper rendering the outgoing page as a frozen `position: fixed` overlay while the incoming page slid in on top, with `prefers-reduced-motion` fallback and the sticky hero-pin hand-off handled. **Jim reviewed it on 2026-09-10 and rejected the slide** — wants a plain hard cut between the two, same as the lightbox decision (`lightbox-no-image-transitions`). All of the transition code (`RouteTransition.jsx`, the `.route-slide` CSS, the `App.jsx` render-prop wiring) was reverted.
  - What was actually wrong, and the fix that shipped: the route change *felt* like scrolling the same page because the global `html { scroll-behavior: smooth }` was animating the post-navigation scroll. On forward nav `ScrollToTop.jsx` did `window.scrollTo(0, 0)` (smooth → animated scroll up through the freshly-mounted case study); on return `Home.jsx` did `el.scrollIntoView()` (smooth → the "half-scroll down from the top" noted here earlier). Both now pass `behavior: 'instant'` (`ScrollToTop.jsx`, and the cross-route `scrollTo` effect in `Home.jsx`), so a route change is a clean hard cut to the right position. In-page nav clicks on the homepage itself still smooth-scroll (unchanged — `navigation.js` / `useSectionNav`).
  - Landing position unchanged: return still lands at the top of `#featured-work` / `#more-work` (whichever section listed the study).
  - `npm run build` clean.

- [x] Unify color tokens around two overarching dark/light pairings — branch: fix/unify-color-tokens
  - Audit + 6 decisions with Jim + implementation, all 2026-09-10. Working doc: `planning/unify-color-tokens.md` (token map, literal-color inventory, decisions log, plan).
  - Outcome by design: **no visible change** — a tokenization/cleanup pass, not a recolor. Decisions: keep navy as its own Profile surface; keep `--color-surface-mid`; keep the near-black photo frame; keep the blue-tinted footer copyright; one `--color-text-muted-dark` for near-black + navy both.
  - `src/index.css` changes: added `--color-text-bright-on-dark: #FFFFFF` (replaces 8 raw `#fff` — PacMan SVG, carousel arrow, More-work CTA, lightbox close/nav, Profile button) and `--color-border: rgba(0,0,0,0.1)` (replaces 5 ad-hoc hairline borders; nav pair went `.08` → `.1`, imperceptible). The 5 `rgba(13,13,18,x)` dark scrims now derive from `--color-surface-dark` via `rgb(from … r g b / x)` (relative color syntax, Baseline 2024). Token block reorganized with comments; documented that `--color-text-primary-dark` === `--color-surface-light` and `--color-nav-bg` === `--color-surface-light` @ 92%.
  - Renamed `--color-text-muted` → `--color-text-muted-light` (8 usages, no value change) so it pairs symmetrically with `--color-text-muted-dark`, matching `--color-text-primary-light` / `-dark`.
  - Also removed the dead no-image thumbnail fallback (`.more-work__thumb-fallback` + its navy→accent gradient in index.css, and the `cs.thumb ?` branch in `MoreCaseStudies.jsx`) — every More card always has a curated image, and `getMore()` already filters out the unlisted entries that lacked one. `--color-surface-navy` now backs the Profile section alone.
  - Rode along on this branch (uses the new token): Intro headline now ends with "Explore my work…" on its own line, set in `--color-text-muted-light`, as a quiet scroll cue. `src/components/Intro.jsx` + `.intro__cue` in index.css.
  - `npm run build` clean.
  - Deferred (separate a11y task): `--color-text-muted-light` `#888` fails WCAG AA on `#F2F0ED` for small text (13px figcaption, 12px nav label); ~`#767676` would fix it. (Superseded 2026-09-18: `#767676` is only 3.99:1 on `#F2F0ED`, see the WCAG contrast item at the top of Backlog.)

- [x] Make the case study lightbox nav circular — branch: fix/case-study-lightbox-nav-is-circular
  - `src/components/Lightbox.jsx`: `goNext`/`goPrev` (shared by the arrows, ← / → keys, and swipe) now wrap with modulo instead of clamping via `Math.min`/`Math.max`. Neither arrow is ever `disabled` — from the last image, next loops to the first, and vice versa. `count <= 1` short-circuits navigation, and for a single-image array the arrows and the `1 / 1` counter aren't rendered at all (previously the arrows rendered in a greyed disabled state). Removed the now-dead `.lightbox__nav:disabled` rule and the `:not(:disabled)` guard on the nav hover rule.
  - `.lightbox__counter` font switched from `--font-mono` (DM Mono) to `--font-sans` (Roboto).
  - Image transitions: none, deliberately. Explored a directional slide (incoming image drifts ~56px in from the side of travel while the outgoing one slides the same way) and then a motionless crossfade (outgoing image held on an overlay, fading out on top of the already-swapped-in new image), tuned across 190–800ms with and without a stagger between out and in. Jim preferred the original hard cut, so all of it was backed out. Navigation is an instant swap; the only lightbox animation is still the 150ms `lightbox-fade` when it opens. If motion is ever wanted here, the crossfade approach (overlay fades out on top, new image swapped in underneath at full opacity so total coverage stays ~1) avoided the luminance dip and, with `will-change: opacity`, the bitmap-reraster shimmer.

- [x] Replace the social-preview (Open Graph / Twitter Card) image — branch: fix/update-social-preview-image
  - Went with a purpose-built, text-free 1200×630 brand mark (the hand + gear outline on the near-black field), not a screenshot or a name/tagline card. Rationale: link unfurls on LinkedIn/Slack/Facebook/Discord/X all render `og:title` + `og:description` next to the image, so the words are already covered; the image just needs to be a recognizable brand mark. Mark is centered so it survives center-square crops (WhatsApp, Google).
  - Asset at `public/assets/brand/social-preview.png` (1200×630, ~25KB, RGB no alpha). Source/working files in `brand/hand gear/` (`hand_gear_1200x630.psd`, vector original `hand_gear.ai`).
  - `index.html`: `og:image` / `twitter:image` → new asset; `og:image:width|height` → 1200/630; added `og:image:alt` + `twitter:image:alt`; `twitter:card` → `summary_large_image`.
  - Also refreshed the shared title + description while here (no em-dash per Jim's style): title `Jim Reed, Product Designer in San Francisco`; description trimmed to `Product Designer creating tools for expert users in complex domains: AI, genomics, industrial operations, and data platforms.` — applied to `<title>`, `og:*`, and `twitter:*` so all three stay in sync.
  - Post-deploy (production, jimreed.net): run the URL through the LinkedIn Post Inspector (https://www.linkedin.com/post-inspector/) to force a re-scrape — LinkedIn caches previews ~7 days. Optionally hit the Facebook Sharing Debugger too. Slack/iMessage/X clear on their own.
  - Follow-up folded into the "Refactor the hand/gear animation to CSS" backlog item: that rebuild should use `social-preview.png` / the `.ai` source as the SVG geometry reference, and set the "UX Design" label to Roboto Slab Medium.

- [x] Curate the More Case Studies thumbnails on the homepage — branch: fix/curate-more-case-studies-thumbnails
  - The 6 listed case studies (operational-insights, developer-experience, machine-learning-for-operators, mobile-emergency-system, additive-manufacturing, analytics-big-data) now have hand-curated 1200×800 (3:2) WebP thumbnails at `public/assets/case-studies/homepage-assets/<id>.webp`; interim `.png`s dropped, `thumb` paths in `caseStudies.json` updated. 1200×800 chosen to clear 3× on every real device (the thumb frame renders at most ~475 CSS px, tablet 2-col).
  - The 3 unlisted case studies (samsung-interactive-tv, banking-experiences, walmart-content) had their `thumb` field removed entirely and their interim `.png`s deleted. They don't render on the homepage grid, and `MoreCaseStudies.jsx` already falls back to a title card when `thumb` is absent. If any is re-listed later it needs a curated thumbnail added back.
  - Inner shadow / rounded corners stay in CSS only (`.more-work__thumb`, `::after`) — nothing baked into the source images.

- [x] Restore + rebuild the More Case Studies section — branch: feature/add-more-case-studies-section
  - Section re-rendered in `Home.jsx`; "More Work" nav link + `#more-work` scroll-spy restored in `Nav.jsx`; the Profile "More Case Studies →" button now scrolls to the section instead of the old site.
  - Cards rebuilt to mirror the featured data model (image, title, short outcome `blurb`, CTA) at a smaller scale. The thumbnail image *is* the CTA: on hover/focus it darkens, reveals a "View Case Study →" label, and scales up ~1.05 — replacing the old card lift/shadow. Rounded corners (12px) and inner shadow are scaled down from the featured carousel's proportions. Tag rendering removed.
  - All 9 non-featured detail pages converted from the Problem/Approach/Outcomes skeleton to the featured `blocks` narrative format, with real production imagery (`homepage-assets/<id>` thumbnails + `<id>/*` section images; PNG after the alpha revert). `outcome`/`images` fields dropped from the non-featured entries.
  - Unlisted (2026-09-09): Samsung Interactive TV, Banking Experiences, and Walmart.com Content carry `"listed": false` in `caseStudies.json` — off the homepage grid and out of the prev/next rotation, but their `/case-study/<id>` pages still resolve on a direct link. Reason: they're early-career agency work (consumer TV / banking / e-commerce) that doesn't fit the site's "tools for expert users in complex domains" positioning, and they're the least-finished of the set. Homepage grid is now 3 featured + 6 more (a clean 2×3). `getMore()` / `getFeatured()` / `getNeighbors()` in `caseStudies.js` filter on `listed !== false`; the case-study page hides its prev/next bar when there are no neighbors. To bring one back: drop the flag and finish the copy (Banking is the strongest candidate).
  - Also in this branch: "← Back to all work" now targets the case study's own homepage section (`#featured-work` or `#more-work`); em-dashes removed from all 12 case studies' copy (Jim's style preference); the About-section "More Case Studies" button removed as redundant.
  - Follow-ups tracked under the "Case study tags", "Case study image assets", and "Refine case study content" backlog items above. The refine pass now has a single working doc: `planning/refine-case-studies.md` (cross-cutting guidance + per-page stubs for all 12).

- [x] Switch DNS for jimreed.net to point at the GitHub Pages site (configure the custom domain + CNAME/A records, enable HTTPS) — branch: chore/switch-dns-to-github-pages
  - Apex + www `A`/`AAAA` records at GoDaddy repointed from Adobe/Fastly to GitHub Pages IPs (wildcard `A` record removed, `www` now a `CNAME` to `jimreedia.github.io`). GitHub Pages custom domain set to `jimreed.net`, HTTPS certificate approved (covers `jimreed.net` + `www.jimreed.net`, expires 2026-12-03), HTTPS enforced. `deploy-production.yml` now passes `cname: jimreed.net` so future production deploys keep the CNAME file. Verified live: `jimreed.net` and `www.jimreed.net` both serve the site over HTTPS.
  - QA found (2026-09-04): the homepage's "More Case Studies" interim link (`jimreedia.myportfolio.com`) was 302-redirecting to `jimreed.net` via Adobe Portfolio's custom-domain setting, which would have looped back to our own homepage after cutover. Resolved by removing the custom-domain setting in Adobe Portfolio — `jimreedia.myportfolio.com` now serves the old site directly, no redirect.

- [x] QA/staging prep for beta launch — two topics: (1) final QA pass on the site as currently live at https://jimreedia.github.io/portfolio/, ahead of switching jimreed.net's DNS to point at it; (2) plan a staging environment for post-beta work — once jimreed.net is live, ongoing changes need a gated build/release process rather than shipping straight to production — branch: chore/qa-beta-launch
  - Staging plan (implemented): split `deploy.yml` into `deploy-staging.yml` (auto-deploys every push to `main`, publishes to `gh-pages:/staging/`, injects `noindex` meta) and `deploy-production.yml` (`workflow_dispatch` only — the manual trigger is the release gate). Staging is reachable at `/staging/` under whichever domain serves the Pages site (github.io URL today, jimreed.net after DNS cutover) — no new hosting/DNS needed, since the build uses a relative Vite `base` and `HashRouter`, so the same artifact works unmodified at any path. Added `public/robots.txt` disallowing `/staging/`.
  - QA pass fixed a duplicate-React-key bug on case study pages, removed stray `[Team placeholder]` text, added Open Graph/Twitter Card meta tags, and fixed a skipped heading level on the homepage.
- [x] Simplify the footer — drop the LinkedIn link (already visible in the About section within the same viewport, so it's redundant), and put the "back to top" link and copyright info on the same row — branch: fix/simplify-footer
- [x] manually curate the carousel images shown in the homepage Featured Case Studies section — choose which shots appear and their order, re-crop as needed, and source new images where the current set falls short — branch: fix/homepage-case-study-images
- [x] Add link to old site — label is "More Case Studies" (phase 1: interim link; phase 2 replaces it with the restored section below) — branch: feature/add-link-to-more-case-studies
- [x] Scroll-linked section transitions (parallax) — deferred from the homepage build; replaces the one-shot fade-up that was removed — branch: feature/add-more-scroll-transitions
- [x] Featured case study contribution subtext — branch: feature/case-study-contribution-subtext
- [x] Adjust typography — branch: fix/adjust-typography
- [x] Animate pacman graphics on user scroll — branch: feature/animate-pacman-on-scroll
- [x] Recolor animated hand/gear graphic — branch: fix/recolor-hand-gear-graphic
- [x] In Nav component, replace the "Jim Reed" title text with an icon; add a favicon so it renders in the browser tab — branch: feature/nav-icon-favicon
- [x] Hide the entire More Case Studies section until we're ready to work on it — branch: chore/hide-more-case-studies
- [x] Design and build case study pages — branch: fix/case-study-pages-init
- [x] Homepage build (6 sections + nav) — branch: feature/homepage-redesign
- [x] Stop committing docs/ build output — branch: chore/ignore-docs-build-output
- [x] Remove case study numbers (01/02/03) from Featured Case Studies — branch: fix/remove-case-study-numbers
- [x] Add featured case study images (currently color-tile placeholders) — branch: fix/add-case-study-images
- [x] Move scroll-down CTA into Section 1 (Brand Imagery) and make it more subtle — branch: fix/move-scroll-down-CTA
- [x] Add workflow_dispatch trigger to deploy workflow — branch: chore/add-deploy-workflow-dispatch
- [x] Adjust Profile section (headshot crop, and other section adjustments as needed) — branch: fix/adjust-profile-section
